import {h} from 'preact';

const CenterContentWrapper = ({children}) => (<div className="wrapCenterCont">
<div className="centeredContX">{children}</div>
</div>)

export default CenterContentWrapper;
